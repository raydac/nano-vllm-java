/**
 * Project-local helpers for demos and tests (model / RAG folder resolution, ordered console,
 * EPUB plain-text extraction, few-shot {@link EmbeddingClassifier} on encoder vectors). Not part of
 * the library API — weights are not shipped in the JAR; {@code pg59112.epub} is a samples classpath
 * resource.
 */

package com.igormaznitsa.nanollvm.samples.utils;
