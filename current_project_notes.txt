## 1. Implementation Progress
- Maven **`1.1.0-SNAPSHOT`** (released **1.0.0** 2026-08-09); enforcer Java `[21,)`, Maven `[3.9,)`; JPMS module `com.igormaznitsa.nanollvm`
- **ResourceLimits** (exported utils): process-wide defaults + builder; RAG/PDF/JSON/GGUF/safetensors/corpus/history budgets
- CausalLM: Qwen3 + Gemma3 + LFM2 (GGUF); tokenizers GPT-2 BPE / Metaspace / GGUF-embedded / BERT WordPiece
- **GGUF BERT embeddings** (e.g. gte-small): same `LlmModelFactory.make` → `LlmModel.embed`; `LLM.builder` rejects embedding models; Q3_K + IQ4_NL dequant
- **ModelFileSource**: `ModelFileId` → `InputStream` (+ weight shards); load into heap only (no disk cache); `make(Path)` stays direct disk I/O
- **GGUF:** packed by default; `LlmModelFactory.make(path, io, true)` unpacks at load; `LLM.Builder.allowUnpackParameters()` late-unpacks packed models
- Library: `LlmModelFactory.make` → shared `LlmModel` (`AutoCloseable`); also `make(ModelFileSource)` / `fromClasspath*` (stream → heap; no disk cache); `LLM.Builder` (warmup **off** by default); close **LLM first**, then **model**


- **Advisors:** `advisors(LlmAdvisorMixer, LlmAdvisor…)`; unique non-blank names; one batched `generate`
- Engine: `Transformer` owns per-LLM arenas + explicit step `Context`; `MatmulRuntime` per `LLM`; `kvHeapFraction` (default 0.25) for KV auto-size
- `LLM.generate` / `close` use `ReentrantLock`; `assertNotClosed` on infrequent entry points; props = **`nanollvm.*` / `NANOLLVM_*` only**
- JPMS: exports models/llm/chat/rag/exceptions/tokenizer/utils; non-exported `models.internal` (`LlmModelAccess`), prompts, samples, engine/layers/tensor/internal

- **RAG:** `RagFactory` → `PreparedRag` (BM25); `addResource` / `makeResource` for classpath docs; optional `withEmbeddings` → `HybridRagIndex` (`DenseRagIndex` + RRF); package-private `CorpusLoader`; public API ~12 types (+ `PdfTextExtractor`)
- Optional download scripts: Qwen3 / Gemma3 / LFM2 GGUF / **gte-small Q2_K** embedding GGUF (`models/download-*.sh`)
- **Samples:** linear `main` flows (`HelloWorld`, `EmbedHelloWorld`, `LogTriageHelloWorld`, `Bench`, `Example`); `Example` menu: model then RAG mode (none / BM25 / dense / hybrid); gte-small embeddings REPL; `BundledModels` / `BundledRag` / `OrderedConsole` in `samples.utils`


## 2. Architecture & Technical Discoveries
- Share one `LlmModel` / `PreparedRag` across many `LLM`s; one LLM not concurrent-safe for generate (`cancel()` is cross-thread)
- Lifecycle: `LLM.close()` frees scheduler + KV/conv arenas + matmul mark (drops network ref); `LlmModel.close()` releases packed payloads / clears weight+network/encoder refs — close engines before model
- `.disableMultiCpu()` / `cpuThreads(1)` → `MatmulRuntime.sequential()` (pool=null, calling thread only); builder thread count wins over `-Dnanollvm.cpu.threads`
- Engine obtains `CausalLM` via `LlmModelAccess.resolveNetwork` (no public `network()`); embedding models hold `EmbeddingEncoder` instead
- BERT GGUF: token+pos+type emb → LayerNorm → post-LN blocks (bidirectional attn + GELU FFN) → mean pool → L2; tokenizer WordPiece with ▁ metaspace pieces
- GGUF packed by default; late unpack does not release bytes still bound by other engines
- `TEXT_DEBUG`: optional via `emitDebugPrompts`; RAG corpus/advisor notes are trusted prompt input by design
- Dense RAG: index-time `LlmModel.embed` of all chunks; query cosine (dot); hybrid = BM25 + dense RRF; off-topic = both indexes agree


## 3. Technical Debt & Refactoring
- Attention O(n²); no float16 / TP / CUDA; GGUF packed by default; HF safetensors still FP32
- Optional GGUF perf: mmap weights; FP16 / smaller default KV; fused packed GEMM
- LFM2 GGUF tokenizer heuristics may need per-export tuning
- Dense weight `float[]` still GC-only after model close (no eager zeroing of Tensor buffers)
- Dense RAG query embeds synchronize per `DenseRagIndex`; no ANN / vector DB yet


## 4. Current Work In Progress
- Development line **`1.1.0-SNAPSHOT`** (post-1.0.0)
- Optional GGUF perf backlog (mmap, FP16, smaller default KV, fused packed GEMM)


## 5. Known Issues & Limitations
- Security: see `security_audit.txt` — defaults cap parsers/corpus; RAG mixes corpus into prompts by design
- CPU-only; weights not in git; LFM2 needs large heap for KV/scratch
- Demo `rag/` is English Grimm/LRRH only
- Advisors add latency (one batched generate); matmul pool is process-shared or caller-provided
- Closing `LlmModel` while an `LLM` still holds the graph leaves dense weights reachable until that LLM closes
- BERT embedding GGUFs only (no HF safetensors BERT directory load yet)
- Model/RAG-dependent unit tests skip via `BundledModelAssumptions` (stderr WARNING + JUnit abort) when weights or `./rag` are absent


## 6. Open Questions & Pending Decisions
- Fold Sampler deeper into Transformer bricks?
- BM25F field weights for multi-field docs?
- HF safetensors LFM2 directory load beside GGUF?


## 7. Useful References
- `security_audit.txt` — 1.0.0 threat-model summary
- `CHANGELOG.md` — 1.0.0 released; active line 1.1.0-SNAPSHOT
- `README.md` — Hello World, CLI, library samples
- Build: `mvn22 -q test` / `MAVEN_OPTS="-Xmx8g" mvn22 -q exec:java`
- Library: `try (LlmModel m = LlmModelFactory.make(path); LLM llm = LLM.builder(m)…)` — engines close before model
- Embeddings: `LlmModelFactory.make(gteGguf); model.embed(text)` — sample `EmbedHelloWorld`
- Dense RAG: `RagFactory.withEmbeddings(preparedRag, embedModel)` → `HybridRagIndex`; keep BM25 via `PreparedRag` alone
- Classpath RAG: `RagFactory.makeResource("docs/a.md")` / `.addResource(loader, path)` / `.addResource(Class, "/abs.md")`
- Props: `nanollvm.*` / `NANOLLVM_*` only (`NanoLlvmProps`)
- Heap/vector: `.mvn/jvm.config`; surefire uses `jvm.module.args`


## 8. User Advice and Recommendations
- Document Maven as `mvn` (portable); agent on this machine may use `mvn22`
- Prefer `ChatSession` for dialog; raw `generate` for completions
- Prefer `LlmModelFactory.make` → `LLM.builder(LlmModel)`; close LLM then model
- Embedding GGUFs use the same factory; call `embed`, not `LLM.builder`
- Dense RAG: prefer `RagFactory.withEmbeddings` / `HybridRagIndex`; keep chat `LLM` and embedding `LlmModel` separate; close embed after the index is unused
- Do **not** update root `README.md` for SNAPSHOT/unreleased work — it documents the last published release; use `CHANGELOG.md` / `models/README.md` / samples until release
- Optional `.matmulExecutor(ExecutorService)`; never shut down by `LLM.close()`
- Optional: use Optional as return only (never method params)
- English `description.md` only
- Unreassigned method/ctor params must be `final`
- Do not add temporary large PDFs under `rag/` into the repo
- New public types/methods after a release must carry `@since X.Y.Z` matching the upcoming version (e.g. `@since 1.1.0` on the 1.1.0 line)
